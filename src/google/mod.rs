//! Google API clients.
//!
//! Phase 2 adds `http` (shared reqwest client + retry) and `session`
//! (in-memory access-token cache with single-flight refresh).
//! Phase 3 adds `gmail` (the GmailClient).
